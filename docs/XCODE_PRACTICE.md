# Xcode Practice

Use this repository in Xcode by opening `Package.swift`.

## Why This Exists

The repository still keeps standalone lesson files for reading and review, but direct hands-on practice now starts from multi-target Objective-C package targets.

Each target is meant to be opened, edited, built, and rerun inside Xcode.

## Current Practice Targets

1. `F01ClassesAndProperties`
2. `F02CategoriesAndProtocols`
3. `F03FoundationObjects`
4. `I01ModelMappingValidation`
5. `I02FilePersistence`
6. `I03OperationPipeline`
7. `I04TestingMindset`
8. `A01RuntimeDispatch`
9. `A02MemoryAndBlocks`
10. `A03KVCAndKVO`
11. `A04DesignPatterns`
12. `A05Concurrency`
13. `P01TaskBoardCLI`

## How To Open

1. open `Package.swift` in Xcode
2. wait for package indexing
3. choose one executable target
4. edit `.h` and `.m` files directly
5. run the target

## Suggested Training Flow

1. start with `F01ClassesAndProperties`
2. move to `F02CategoriesAndProtocols`
3. move to `F03FoundationObjects`
4. move to `I01ModelMappingValidation`
5. move to `I02FilePersistence`
6. move to `I03OperationPipeline`
7. move to `I04TestingMindset`
8. move to `A01RuntimeDispatch`
9. move to `A02MemoryAndBlocks`
10. move to `A03KVCAndKVO`
11. move to `A04DesignPatterns`
12. move to `A05Concurrency`
13. finish with `P01TaskBoardCLI`

## What To Change First

- add one property
- add one method
- split one responsibility into a second class
- add one more branch or print path
- turn one lesson change into a project change
